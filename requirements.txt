# --- Qwen comparison backend and optional anime forced-aligner diagnostics ---
# Pulls in torch / transformers / librosa / soundfile. The PyPI torch wheel may not
# match very new GPUs: for NVIDIA Blackwell (RTX 50-series) install a CUDA 12.8 build
# first, e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchaudio
qwen-asr==0.0.6

# --- Default anime ASR backend: WhisperSeg ONNX weak-speech VAD ---
# whisperseg_vad.py imports onnxruntime directly and is NOT pulled in by qwen-asr.
# GPU wheel matches the CUDA anime default; on a CPU-only host use `onnxruntime` instead.
# scikit-learn / scipy (semantic scene clustering) arrive transitively via librosa.
onnxruntime-gpu==1.27.0

huggingface-hub==0.36.2
pykakasi==2.3.0
# NOTE: PyPI wheel is CPU-only. For NVIDIA GPU, rebuild after install:
#   CMAKE_ARGS='-DGGML_CUDA=on' FORCE_CMAKE=1 \
#     pip install --force-reinstall --no-cache-dir llama-cpp-python==0.3.23
llama-cpp-python==0.3.23
